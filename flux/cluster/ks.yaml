# DEPRECATED — this file is superseded.
#
# The canonical Flux entry point is now generated by `task configure`:
#   kubernetes/flux/cluster/ks.yaml
#
# Do NOT edit this file manually. Update cluster.yaml extras list and
# re-run `task configure` instead.
#
# For a reference/sample, see per-user-repo.sample.yaml at the repo root.
